<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:supportsRtl="true"
        android:theme="@style/Theme.Pokedex">

        <activity
            android:name=".feature.list.presentation.HomeActivity"
            android:exported="true"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".feature.about.presentation.AboutActivity"
            android:launchMode="singleTask"
            android:theme="@style/Theme.Pokedex.NoActionBar" />

        <activity
            android:name=".feature.search.presentation.SearchActivity"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustNothing" />

    </application>

</manifest>