<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:name="fr.o80.pokedex.lib.EverythingIsFine"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Pokedex.Charmander"
        tools:replace="theme">

        <activity
            android:name="fr.o80.pokedex.lib.feature.about.presentation.AboutActivity"
            android:theme="@style/Theme.Pokedex.Charmander.NoActionBar"
            tools:replace="theme" />

    </application>

</manifest>